#include <stdio.h>

int main(){
    
int i; //a variavel i é do tipo "inteiros"
char ch1, novo_char; //as variaveis ch1 e novo_char são do tipo "caracteres" porem so aceita exatamente 1 caractere.
float pi, raio, perimetro; // as variaveis pi, raio, perimetro são do tipo "reais"
double total, k123; // As variaveis total, k123 tbm são do tipo "reais"

/*Valor inteiros(int) são declarados usando o simbolo %d.
  Valores Reais(float / double) são declarados usando o simbolo %f.
  Valores caracteres(char) são declarados usando o simbolo %c (para darmos algum valor ja na criação de variavel do tipo CHAR devemos colocar entre '' e n entre "")
*/

}